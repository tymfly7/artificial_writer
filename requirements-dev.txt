# Development environment: core + web + optional LLM providers + tooling.
# For an editable install with the same set, prefer:  pip install -e ".[all,dev]"
-r requirements.txt

# Web interface
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# Optional LLM providers
openai>=1.30
anthropic>=0.40

# Tooling
pytest>=8.0
pytest-cov>=5.0
responses>=0.25
ruff>=0.5
mypy>=1.10
types-requests>=2.31
