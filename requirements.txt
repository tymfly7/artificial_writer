# Core runtime dependencies (the app runs free & offline with just these).
# Authoritative source is pyproject.toml; this file mirrors it for convenience:
#   pip install -r requirements.txt
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
